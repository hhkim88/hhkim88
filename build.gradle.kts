// Intentionally empty: each subproject declares its own plugins with explicit
// versions so that `:core` (a plain Kotlin/JVM module) can be built and tested
// without ever needing to resolve the Android Gradle Plugin.
